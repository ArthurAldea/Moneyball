---
phase: 05-dashboard-rebuild-shortlist-filters
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - test_app.py
autonomous: true
requirements:
  - FILTER-01
  - FILTER-02
  - FILTER-03
  - FILTER-04
  - FILTER-05
  - FILTER-06
  - DASH-01
  - DASH-02
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07

must_haves:
  truths:
    - "Every filter function has a failing test before implementation begins"
    - "test_app.py is importable and runnable via pytest with no StreamlitAPIException"
    - "Fixture factory produces a DataFrame matching run_scoring_pipeline output schema"
  artifacts:
    - path: "test_app.py"
      provides: "12 unit tests covering FILTER-01–06, DASH-01, DASH-02, DASH-04–07"
      min_lines: 120
  key_links:
    - from: "test_app.py"
      to: "app.py (filter logic functions)"
      via: "direct Python import of apply_filters(), scatter_chart(), NAVY_CSS"
      pattern: "from app import"
---

<objective>
Create the test scaffold for Phase 5 before any app.py implementation begins. All 12 tests must be RED (ImportError or assertion failure) after this plan — they become GREEN in Plan 05-02.

Purpose: Establish verifiable contracts for every requirement. The filter logic, chart construction, and CSS constants must be importable and testable without running Streamlit.

Output: test_app.py with make_pipeline_df fixture factory and 12 unit tests.
</objective>

<execution_context>
@/Users/ArthurAldea/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ArthurAldea/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-rebuild-shortlist-filters/05-RESEARCH.md
@.planning/phases/05-dashboard-rebuild-shortlist-filters/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test_app.py — fixture factory + 12 failing unit tests</name>
  <files>test_app.py</files>
  <read_first>
    - /Users/ArthurAldea/ClaudeProjects/Moneyball/test_scorer.py (fixture patterns used in this project)
    - /Users/ArthurAldea/ClaudeProjects/Moneyball/app.py (current app — understand what will be imported from the rewrite)
    - /Users/ArthurAldea/ClaudeProjects/Moneyball/scorer.py (confirm run_scoring_pipeline output columns: Player, Squad, Pos, Age, League, market_value_eur, scout_score, uv_score, uv_score_age_weighted, value_gap_eur, league_quality_multiplier, predicted_log_mv, similar_players)
  </read_first>

  <behavior>
    The tests import pure-Python functions from app.py directly — no Streamlit server running. The rewrite in Plan 05-02 will extract these as module-level functions. All tests must be RED after this plan (ImportError from app because functions don't exist yet), GREEN after Plan 05-02.

    Fixture factory `make_pipeline_df(n=20)` must produce a DataFrame with these exact columns:
      Player (str), Squad (str), Pos (str, values from ["FW","MF","DF","GK"]), Age (str, "25-201" FBref format),
      League (str, values from ["EPL","LaLiga","Bundesliga","SerieA","Ligue1"]),
      market_value_eur (float, raw EUR e.g. 15_000_000.0),
      scout_score (float, 0–100), uv_score (float), uv_score_age_weighted (float),
      value_gap_eur (float), league_quality_multiplier (float, e.g. 1.10),
      predicted_log_mv (float, log10 of market_value_eur), similar_players (str, "[]"),
      _season (str, values from ["2023-24","2024-25"])

    12 required tests:

    test_filter_league:
      - Given full_df with players from EPL and LaLiga, apply_filters(full_df, leagues=["EPL"], ...) returns only EPL players.

    test_filter_position:
      - apply_filters(full_df, positions=["FW"]) returns only FW players.

    test_filter_age:
      - apply_filters(full_df, age_range=(20, 25), ...) excludes players outside 20–25 float age range.
      - Age strings "24-201" and "26-100" must be correctly converted — "24-201" passes, "26-100" excluded.

    test_club_options_derived_from_leagues:
      - get_available_clubs(full_df, leagues=["EPL"]) returns only clubs present in EPL rows, not clubs from LaLiga rows.

    test_filter_market_value:
      - apply_filters(full_df, mv_range=(0, 20), ...) excludes players with market_value_eur > 20_000_000.
      - Confirm slider €M values are multiplied by 1e6 inside apply_filters.

    test_filter_season:
      - apply_filters(full_df, seasons=["2023-24"], ...) returns only rows where _season == "2023-24".
      - If _season column absent from df, apply_filters returns df unchanged (defensive skip).

    test_default_sort_order:
      - prepare_display_df(full_df) returns DataFrame sorted by uv_score_age_weighted descending.
      - First row has the highest uv_score_age_weighted value.

    test_display_columns:
      - prepare_display_df(full_df) returns DataFrame with exactly these columns (in any order):
        Player, Squad, League, Pos, Age, scout_score, uv_score, uv_score_age_weighted, market_value_eur, value_gap_eur
      - market_value_eur values are in €M (divided by 1e6).

    test_row_selection_index:
      - Given display_df with 5 rows (reset_index), selecting row index 0 returns the player with highest uv_score_age_weighted.
      - Assert display_df.iloc[0]["Player"] matches the player with max uv_score_age_weighted in original df.

    test_css_contains_navy_theme:
      - NAVY_CSS string (exported from app.py) contains "#0D1B2A" (background).
      - NAVY_CSS contains "#00A8FF" (accent).
      - NAVY_CSS contains "Inter" (font).

    test_scatter_axes:
      - scatter_chart(df) returns a go.Figure.
      - Figure has at least one trace using x=scout_score values.
      - Figure has at least one trace using predicted_log_mv values as y.
      - Figure has a trace named "FAIR VALUE LINE".

    test_cross_league_disclaimer_condition:
      - should_show_disclaimer(selected_leagues=["EPL"]) returns False.
      - should_show_disclaimer(selected_leagues=["EPL", "LaLiga"]) returns True.
      - should_show_disclaimer(selected_leagues=[]) returns False.
  </behavior>

  <action>
    Create /Users/ArthurAldea/ClaudeProjects/Moneyball/test_app.py from scratch.

    File structure:
    1. Module docstring: "Tests for app.py Phase 5 dashboard — filter logic, chart functions, CSS constants."
    2. Imports: pytest, pandas as pd, numpy as np, plotly.graph_objects as go
    3. Attempt to import from app: `from app import apply_filters, get_available_clubs, prepare_display_df, scatter_chart, should_show_disclaimer, NAVY_CSS`
       — This import will raise ImportError until Plan 05-02 adds these names. That is expected (RED state).
    4. Fixture `make_pipeline_df(n=20)` as a pytest fixture using `@pytest.fixture`. Also provide a standalone function with the same name so tests can call it with custom n.
    5. The fixture creates a deterministic DataFrame:
       - n rows, cycling through positions ["FW","MF","DF","GK"]
       - cycling through leagues ["EPL","LaLiga","Bundesliga","SerieA","Ligue1"]
       - Ages as strings: "22-150", "24-201", "26-100", "28-050", "30-200" cycling
       - market_value_eur: [5e6, 10e6, 15e6, 20e6, 25e6, 30e6, ...] cycling
       - scout_score: float range 40–90 evenly spaced
       - uv_score: scout_score / predicted_log_mv
       - uv_score_age_weighted: uv_score * 1.1 for first half, uv_score for second half
       - value_gap_eur: market_value_eur * 0.1 (positive = undervalued)
       - league_quality_multiplier: 1.10 for EPL rows, 1.00 for others
       - predicted_log_mv: np.log10(market_value_eur)
       - similar_players: "[]"
       - _season: "2023-24" for even indices, "2024-25" for odd indices
       - Squad: f"Club{i % 10}" to give variety
       - Player: f"Player{i}"
    6. Write each of the 12 tests exactly as described in the behavior section.
    7. Use direct assertions — no mocking of Streamlit. All tested functions are pure Python.

    Key implementation note for test_scatter_axes:
      Create a small df from make_pipeline_df(10), call scatter_chart(df), then inspect figure.data:
      - Check any(t.x is not None and len(t.x) > 0 for t in fig.data) and the x values match scout_score
      - Check any(t.name == "FAIR VALUE LINE" for t in fig.data)
      - For y-axis check: sub_traces = [t for t in fig.data if t.name != "FAIR VALUE LINE"]; check sub_traces[0].y values are log-scale (all < 10, since log10(25e6) ~ 7.4)
  </action>

  <verify>
    <automated>cd /Users/ArthurAldea/ClaudeProjects/Moneyball && venv/bin/python -m pytest test_app.py -q --tb=short 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - test_app.py file exists at /Users/ArthurAldea/ClaudeProjects/Moneyball/test_app.py
    - File is at least 120 lines long
    - Running `venv/bin/python -m pytest test_app.py --collect-only -q` shows exactly 12 test items collected
    - Running `venv/bin/python -m pytest test_app.py -q --tb=short` exits with a non-zero code (tests fail because app.py doesn't export the required functions yet — this is the expected RED state)
    - No SyntaxError when running `venv/bin/python -c "import ast; ast.parse(open('test_app.py').read())"` — file is valid Python
    - test_app.py contains the string "from app import" (imports from app.py directly, not via Streamlit server)
    - test_app.py contains "make_pipeline_df" (fixture factory present)
    - test_app.py contains "_season" (season filter test present)
    - test_app.py contains "FAIR VALUE LINE" (scatter axis test present)
    - test_app.py contains "NAVY_CSS" (CSS theme test present)
  </acceptance_criteria>

  <done>test_app.py exists with 12 RED unit tests and a fixture factory. Running pytest shows collection of 12 items, all failing with ImportError. Existing test suite (test_scorer.py, test_merger.py, test_scraper.py) still passes.</done>
</task>

</tasks>

<verification>
- `venv/bin/python -m pytest test_app.py --collect-only -q` → exactly 12 tests collected
- `venv/bin/python -m pytest test_scorer.py test_merger.py test_scraper.py -q --tb=short` → all green (existing tests unaffected)
- `venv/bin/python -c "from test_app import make_pipeline_df; df = make_pipeline_df(20); print(df.columns.tolist())"` → prints column list including _season and predicted_log_mv
</verification>

<success_criteria>
- test_app.py created with 12 test functions + make_pipeline_df fixture
- All 12 tests fail with ImportError (RED — app.py doesn't export required functions yet)
- Existing 37 tests pass unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-rebuild-shortlist-filters/05-01-SUMMARY.md`
</output>
