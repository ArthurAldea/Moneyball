---
phase: 05.1-fix-fbref-scraping-replace-requests-with-playwright-to-bypass-cloudflare-js-challenge
plan: "02"
subsystem: scraper
tags: [playwright, cloudflare, fbref, app, load_data, test_data]

# Dependency graph
requires:
  - phase: 05.1-01
    provides: Playwright-based _playwright_fetch replacing requests in scraper.py
provides:
  - load_data() in app.py always runs the real pipeline — no test_data.csv short-circuit
affects: [app.py, production deployments, any future test setup that might re-introduce a test shim]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - app.py

key-decisions:
  - "test_data.csv shim removed: load_data() must never silently serve stale test data in production; the shim was only valid during Phase 5 test setup before the real scraper was fixed"

patterns-established: []

requirements-completed:
  - DATA-06

# Metrics
duration: 25min
completed: 2026-03-17
---

# Phase 05.1 Plan 02: Remove test_data.csv Shim Summary

**load_data() in app.py now unconditionally calls run_fbref_scrapers + run_tm_scrapers + run_scoring_pipeline — the test_data.csv bypass is gone and cache/test_data.csv deleted**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-03-17T19:00:00Z
- **Completed:** 2026-03-17T19:25:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Removed the 4-line `os.path.exists(test_data.csv)` guard from `load_data()` in app.py
- Deleted `cache/test_data.csv` which was the test bypass file
- `load_data()` now starts directly with `from scraper import run_fbref_scrapers, run_tm_scrapers`
- 26 tests pass (14 scraper + 12 app)

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove test_data.csv shim from load_data()** - `3810508` (feat)

**Plan metadata:** (to be committed with this summary)

## Files Created/Modified
- `app.py` - Removed import os, _test_csv, os.path.exists guard, and import pandas as _pd from load_data()

## Decisions Made
- None - followed plan as specified. The target state was exactly as documented in the plan's `<interfaces>` block.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Installed missing Python packages in project venv**
- **Found during:** Task 1 (verification)
- **Issue:** Test suite failed to import scraper.py because `playwright` and `curl_cffi` packages were missing from the active Python environment (anaconda base, not project venv). This was an environment setup issue, not a code issue.
- **Fix:** Identified project venv at `venv/bin/python` which had all required packages; ran tests using venv Python. Also installed `playwright` and `curl_cffi` in the base env as fallback.
- **Files modified:** None (environment config, not code)
- **Verification:** `venv/bin/python -m pytest test_scraper.py test_app.py -x -q` exits 0 with 26 passed
- **Committed in:** N/A (environment, not code)

**2. [Out of scope - pre-existing] Merger and scorer tests hang on network calls**
- **Found during:** Verification
- **Issue:** `test_merger.py::test_min_minutes_threshold_1800` and similar tests call `build_dataset` which calls `attach_league_position` which calls `scrape_fbref_standings` via Playwright (real network request to FBref). These tests hang indefinitely.
- **Fix:** Not fixed — pre-existing condition unrelated to this plan's changes. Logged here for awareness.
- **Impact on this plan:** None. The plan's success criteria require `test_app.py` and `test_scraper.py` to pass, which they do. The merger/scorer test hangs are a pre-existing issue from the network-calling tests added in prior phases.
- **Deferred to:** deferred-items.md

---

**Total deviations:** 1 auto-fixed (blocking environment issue)
**Impact on plan:** Auto-fix was to use the project venv for test execution. No scope creep.

## Issues Encountered
- Tests hung at test 22 of 63 when run through the full suite. Root cause: `test_min_minutes_threshold_1800` calls `build_dataset` which triggers `scrape_fbref_standings` via Playwright (real network call). This is a pre-existing issue not introduced by this plan.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 05.1 is complete: both plans done (01 = Playwright scraper, 02 = test shim removal)
- app.py load_data() is clean — no test bypasses remain
- Ready to proceed to Phase 5 Dashboard Rebuild

---
*Phase: 05.1-fix-fbref-scraping-replace-requests-with-playwright-to-bypass-cloudflare-js-challenge*
*Completed: 2026-03-17*
