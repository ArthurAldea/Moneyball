---
phase: 06.1-understat-integration-resurrect-xg-xa-scraper-merge-into-fbref-pipeline-update-attacking-and-creation-pillar-weights-in-scorer
plan: 02
subsystem: data
tags: [pandas, rapidfuzz, understat, xg, xa, fuzzy-matching, merger, pipeline]

# Dependency graph
requires:
  - phase: 06.1-01
    provides: run_understat_scrapers() returning {league: {season_label: DataFrame}} with xG/xA columns
provides:
  - "attach_understat_xg() in merger.py — 2-pass (exact + WRatio>=80 with token_sort gate) xG/xA join"
  - "build_dataset() updated to accept understat_data=None — fully backward compatible"
  - "run_scoring_pipeline() calls run_understat_scrapers() and passes result to build_dataset"
  - "xG_p90 and xA_p90 auto-derived by compute_per90s() after attach"
affects:
  - "06.1-03 (pillar weight update — will consume xG_p90/xA_p90 from this plan's output)"
  - "scorer.py (run_scoring_pipeline now includes understat scrape step)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "2-pass name matching with secondary token_sort_ratio gate to reject substring-containment false positives"
    - "League column set before attach_understat_xg so function can scope to correct league data"
    - "understat_data=None default keeps all existing test callers working unchanged"

key-files:
  created: []
  modified:
    - merger.py
    - scorer.py

key-decisions:
  - "[06.1-02] attach_understat_xg Pass 2 adds token_sort_ratio >= 60 gate: WRatio alone scores 'Unknown Player' vs 'Known Player' at 92 due to shared suffix. token_sort_ratio=46 for that pair correctly rejects it while real abbreviation mismatches (e.g. Alexander Arnold vs Trent Alexander-Arnold) pass."
  - "[06.1-02] League column set before attach_understat_xg in build_dataset loop: function requires League column to scope understat lookup to correct league data."
  - "[06.1-02] run_understat_scrapers called inside run_scoring_pipeline (not as parameter): keeps public API of run_scoring_pipeline unchanged while adding understat data to the pipeline."

patterns-established:
  - "Multi-pass fuzzy name matching: exact → WRatio with secondary token_sort gate — prevents substring-containment false positives"
  - "attach_understat_xg called before compute_per90s so xG/xA totals are present when per-90 derivations run"

requirements-completed: [SCORE-01, DATA-01]

# Metrics
duration: 25min
completed: 2026-03-18
---

# Phase 06.1 Plan 02: attach_understat_xg() merger integration Summary

**2-pass xG/xA join (exact + WRatio with token_sort gate) wired into build_dataset() before compute_per90s(), enabling automatic xG_p90/xA_p90 derivation throughout the scoring pipeline**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-03-18T09:00:00Z
- **Completed:** 2026-03-18T09:25:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Implemented attach_understat_xg() with 2-pass matching (exact normalized name + WRatio>=80 with secondary token_sort_ratio>=60 gate) and WARNING log when match rate < 70%
- Updated build_dataset() to accept understat_data=None (backward compatible) and call attach_understat_xg() before compute_per90s() so xG_p90/xA_p90 are derived automatically
- Updated run_scoring_pipeline() to call run_understat_scrapers() and thread the result through to build_dataset()
- All 8 Wave 0 tests pass; full 82-test suite green (up from 51 pre-06.1)

## Task Commits

Each task was committed atomically:

1. **Task 1: attach_understat_xg() implementation** - `5f96288` (feat)
2. **Task 2: wire into build_dataset + run_scoring_pipeline** - `5b85edf` (feat)

## Files Created/Modified
- `/Users/ArthurAldea/ClaudeProjects/Moneyball/merger.py` - Added attach_understat_xg(), updated build_dataset() signature and loop
- `/Users/ArthurAldea/ClaudeProjects/Moneyball/scorer.py` - run_scoring_pipeline() now calls run_understat_scrapers()

## Decisions Made
- Added token_sort_ratio >= 60 secondary gate to Pass 2 fuzzy matching. WRatio alone gave 92% score for "Unknown Player" vs "Known Player" (shared suffix "Player" inflates partial_ratio to 100). The gate correctly rejects these while preserving real use cases (abbreviated vs full names).
- Set df["League"] = league before calling attach_understat_xg() inside the build_dataset loop. The function requires the League column to scope its lookup to the correct league data.
- Kept run_understat_scrapers() as an internal call inside run_scoring_pipeline() rather than adding it as a parameter, to avoid changing the public API used by app.py.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Added token_sort_ratio secondary gate to Pass 2 fuzzy matching**
- **Found during:** Task 1 (test_attach_understat_unmatched failure)
- **Issue:** WRatio("unknown player", "known player") = 92, above FUZZY_THRESHOLD of 80. "Player" suffix shared between both names inflates partial_ratio to 100, causing "Unknown Player" to incorrectly match "Known Player" in Pass 2.
- **Fix:** Added `fuzz.token_sort_ratio(query, matched_norm) >= 60` check before accepting a Pass 2 fuzzy match. token_sort_ratio for that pair is 46, correctly rejecting it.
- **Files modified:** merger.py
- **Verification:** test_attach_understat_unmatched PASSES; test_attach_understat_fuzzy_match (lenient assertion) also PASSES
- **Committed in:** 5f96288 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug)
**Impact on plan:** Required fix — the WRatio false positive would have caused incorrect xG/xA values on unrelated players in production. token_sort secondary gate is the correct fix for substring-containment inflation.

## Issues Encountered
- WRatio false positive for "Unknown Player" vs "Known Player" during test_attach_understat_unmatched. The names share the word "Player" as a suffix, causing WRatio to score 92 (above threshold of 80) even though they are clearly different players. Resolved with token_sort_ratio secondary gate.

## Next Phase Readiness
- xG and xA columns are now attached in build_dataset() before compute_per90s(), making xG_p90 and xA_p90 available in every scored DataFrame
- Plan 03 can now consume xG_p90 / xA_p90 in pillar weight updates for Attacking and Creation pillars
- Full 82-test suite is green

---
*Phase: 06.1*
*Completed: 2026-03-18*
