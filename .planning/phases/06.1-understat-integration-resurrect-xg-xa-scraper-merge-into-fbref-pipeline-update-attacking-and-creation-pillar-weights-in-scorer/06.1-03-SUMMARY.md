---
phase: 06.1-understat-integration-resurrect-xg-xa-scraper-merge-into-fbref-pipeline-update-attacking-and-creation-pillar-weights-in-scorer
plan: "03"
subsystem: scoring
tags: [xG, xA, understat, pillars, config, scorer, weights]

# Dependency graph
requires:
  - phase: 06.1-02
    provides: attach_understat_xg merger step that populates xG_p90/xA_p90 columns in the scored DataFrame
provides:
  - PILLARS_FW attacking updated to xG_p90 (0.45) + SoT_p90 (0.35) + Ast_p90 (0.20)
  - PILLARS_FW creation updated to xA_p90 (0.50) + Ast_p90 (0.30) + Crs_p90 (0.20)
  - PILLARS_MF attacking updated to xG_p90 (0.40) + Gls_p90 (0.35) + SoT_p90 (0.25)
  - PILLARS_MF creation updated to xA_p90 (0.50) + Ast_p90 (0.30) + Crs_p90 (0.20)
  - Weight-sum integrity tests (test_fw_pillar_weights_sum, test_mf_pillar_weights_sum)
  - Updated CLAUDE.md pillar table and FBref Lit GONE section
affects: [scorer, app, pillar display, scout_score computation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - FW/MF creation pillars use explicit inline dicts (not _CREATION spread) to allow different stats from DF creation
    - Pillar weight-sum tests enforce 1.0 sum invariant across all position groups

key-files:
  created: []
  modified:
    - config.py
    - test_scorer.py
    - CLAUDE.md

key-decisions:
  - "FW creation/MF creation use explicit inline dicts not {**_CREATION} spread — allows FW/MF to use xA_p90 while DF creation stays on Ast_p90+Crs_p90 via unchanged _CREATION"
  - "test_per_league_normalization_isolation fixture updated to include xG_p90/xA_p90 columns — required because FW attacking pillar now depends on those columns"

patterns-established:
  - "Pillar weight-sum tests (test_fw_pillar_weights_sum, test_mf_pillar_weights_sum) enforce 1.0 constraint for all future pillar changes"

requirements-completed: [SCORE-01]

# Metrics
duration: 8min
completed: 2026-03-18
---

# Phase 06.1 Plan 03: Pillar Weight Update (xG_p90/xA_p90) Summary

**FW/MF Attacking and Creation pillars rewired to use xG_p90 and xA_p90 from Understat, completing Phase 06.1 — prolific scorers like Haaland now rewarded via 45% xG weight in FW Attacking**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-18T09:47:11Z
- **Completed:** 2026-03-18T09:55:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added `test_fw_pillar_weights_sum` and `test_mf_pillar_weights_sum` to enforce 1.0 weight-sum invariant across all FW/MF pillars
- Updated all four target pillar weight dicts in config.py with xG_p90/xA_p90; PILLARS_DF and GK_PILLARS left unchanged
- CLAUDE.md pillar model table and GONE section updated to reflect Understat-sourced xG/xA
- Full test suite: 84 tests, all passing

## Task Commits

Each task was committed atomically:

1. **Task 1: Wave 0 scorer stubs — test_scorer.py (2 weight-sum tests)** - `d741540` (test)
2. **Task 2: config.py — update FW and MF pillar weights with xG_p90/xA_p90** - `9fa1a28` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `/Users/ArthurAldea/ClaudeProjects/Moneyball/config.py` - FW/MF Attacking and Creation pillar stats updated; FW/MF creation are now explicit inline dicts not _CREATION spreads
- `/Users/ArthurAldea/ClaudeProjects/Moneyball/test_scorer.py` - 2 weight-sum integrity tests appended; per-league normalization fixture updated with xG_p90/xA_p90 columns
- `/Users/ArthurAldea/ClaudeProjects/Moneyball/CLAUDE.md` - Pillar model table updated; xG/xA GONE entry revised to document Understat sourcing

## Decisions Made
- FW/MF creation pillars replaced `{**_CREATION, "weight": N}` spreads with explicit inline dicts to allow different stats from DF creation (which keeps `Ast_p90 + Crs_p90` via unchanged `_CREATION`).
- `test_per_league_normalization_isolation` fixture updated to include `xG_p90` and `xA_p90` columns — FW attacking now depends on these columns and the test would otherwise score 0 for the attacking pillar, causing a spurious assertion failure.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated per-league normalization test fixture to include xG_p90/xA_p90**
- **Found during:** Task 2 (config.py pillar weight update)
- **Issue:** `test_per_league_normalization_isolation` synthetic data lacked `xG_p90`/`xA_p90` columns. After FW attacking switched from `Gls_p90` to `xG_p90`, the scorer logged "Missing columns (scored 0): {xA_p90, xG_p90}" and A1 scored 69.8 instead of the expected >80, failing the assertion.
- **Fix:** Added `xG_p90` and `xA_p90` rows to both league_a and league_b DataFrames in the test fixture with realistic values matching the relative performance spread of other stats.
- **Files modified:** test_scorer.py
- **Verification:** Test passes (A1 score >80, B1 score >80 with per-league normalization); full suite 84/84 green.
- **Committed in:** `9fa1a28` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug)
**Impact on plan:** Necessary correctness fix — the test was implicitly testing the old pillar columns. No scope creep.

## Issues Encountered
None beyond the auto-fixed test fixture above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 06.1 complete: Understat scraper (Plan 01), FBref merger integration (Plan 02), and pillar weight update (Plan 03) all done.
- xG_p90/xA_p90 flow end-to-end: scraped from Understat → merged by player name → weighted into FW/MF Attacking and Creation scout scores.
- 84 tests all green; _CREATION shared dict preserved for DF; no regressions.

---
*Phase: 06.1-understat-integration*
*Completed: 2026-03-18*
