---
phase: 06.1-understat-integration
verified: 2026-03-18T10:15:00Z
status: passed
score: 8/8 must-haves verified
re_verification: false
---

# Phase 06.1: Understat Integration Verification Report

**Phase Goal:** Resurrect xG and xA per-90 stats from Understat for all 5 leagues x 2 seasons, merge into the FBref pipeline, and update Attacking and Creation pillar weights in the scorer to incorporate xG_p90 and xA_p90 — improving the model's ability to reward prolific scorers like Haaland.
**Verified:** 2026-03-18T10:15:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `scrape_understat_league(league, season_year, season_label)` produces DataFrame with Player, Squad, Min, xG, xA columns; cache file named `cache/understat_{league}_{season}.csv` | VERIFIED | Function exists at scraper.py:731. Cache key confirmed as `understat_EPL_2024-25` via live probe. `test_understat_league_columns` and `test_understat_cache_naming` both PASS. |
| 2 | `run_understat_scrapers()` covers all 5 leagues x 2 seasons (10 scrape calls) | VERIFIED | Real implementation at scraper.py:790 iterates `FBREF_LEAGUES` x `FBREF_SEASONS`. `test_run_understat_all_leagues` PASSES — confirms exactly 10 calls, correct league set, correct season set. |
| 3 | `attach_understat_xg()` joins xG/xA onto FBref merged DataFrame; unmatched players get NaN (not dropped); match rate below 70% triggers WARNING log | VERIFIED | Function at merger.py:370. `test_attach_understat_exact_match`, `test_attach_understat_fuzzy_match`, `test_attach_understat_unmatched` all PASS. Warning logging confirmed in implementation (line 461-467). |
| 4 | `compute_per90s()` produces xG_p90 and xA_p90 automatically after xG/xA are present | VERIFIED | `xG` and `xA` confirmed in `PER90_STATS` in config.py. `test_per90s_xg_xa` PASSES — xG_p90=0.90 and xA_p90=0.45 computed correctly. |
| 5 | `PILLARS_FW` attacking: xG_p90 (0.45), SoT_p90 (0.35), Ast_p90 (0.20); creation: xA_p90 (0.50), Ast_p90 (0.30), Crs_p90 (0.20) | VERIFIED | Confirmed via `python -c "from config import PILLARS_FW; ..."`. Exact values match. |
| 6 | `PILLARS_MF` attacking: xG_p90 (0.40), Gls_p90 (0.35), SoT_p90 (0.25); creation: xA_p90 (0.50), Ast_p90 (0.30), Crs_p90 (0.20) | VERIFIED | Confirmed via `python -c "from config import PILLARS_MF; ..."`. Exact values match. |
| 7 | All pillar weight dicts sum to 1.0; DF and GK pillars unchanged | VERIFIED | All 15 FW/MF/DF pillar stat weight dicts sum to 1.0 (OK). DF attacking still uses `Gls_p90/Ast_p90/SoT_p90`; DF creation still uses `Ast_p90/Crs_p90`. `test_fw_pillar_weights_sum` and `test_mf_pillar_weights_sum` both PASS. |
| 8 | Full test suite (test_scraper.py test_merger.py test_scorer.py test_app.py) entirely green | VERIFIED | 84 passed, 0 failed, 8 warnings. All 10 new phase-specific tests pass alongside all 74 pre-existing tests. |

**Score: 8/8 truths verified**

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `config.py` | UNDERSTAT_LEAGUE_NAMES, xG/xA in SUM_STATS and PER90_STATS, updated FW/MF pillars | VERIFIED | All present. 5-entry league map with correct slugs (EPL, La_liga, Bundesliga, Serie_A, Ligue_1). xG and xA in both SUM_STATS and PER90_STATS. |
| `scraper.py` | `scrape_understat_league`, `_fetch_understat_league`, real `run_understat_scrapers` | VERIFIED | All three functions exist (lines 695, 731, 790). `scrape_understat_season` preserved for backward compat. No DEPRECATED marker on `run_understat_scrapers`. |
| `merger.py` | `attach_understat_xg`, updated `build_dataset(understat_data=None)` | VERIFIED | `attach_understat_xg` at line 370. `build_dataset` signature updated (line 481-482). Call site at line 523 inside per-league loop, before `compute_per90s`. |
| `scorer.py` | `run_understat_scrapers` called in `run_scoring_pipeline` | VERIFIED | Lines 338-340: import and call present, result passed to `build_dataset(understat_data=understat_data)`. |
| `test_scraper.py` | 4 Wave 0 Understat scraper tests | VERIFIED | `test_understat_league_columns`, `test_understat_cache_naming`, `test_understat_cache_hit`, `test_run_understat_all_leagues` — all PASS. |
| `test_merger.py` | 4 Wave 0 Understat merger tests | VERIFIED | `test_attach_understat_exact_match`, `test_attach_understat_fuzzy_match`, `test_attach_understat_unmatched`, `test_per90s_xg_xa` — all PASS. |
| `test_scorer.py` | 2 weight-sum integrity tests | VERIFIED | `test_fw_pillar_weights_sum` and `test_mf_pillar_weights_sum` — both PASS. |
| `CLAUDE.md` | Updated pillar model table and Understat xG/xA sourcing note | VERIFIED | Pillar table rows updated for Attacking and Creation with xG_p90/xA_p90 weights. GONE section replaced with explicit Understat sourcing note. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `scrape_understat_league` | `UNDERSTAT_LEAGUE_NAMES[league]` | slug lookup in config.py | VERIFIED | `_fetch_understat_league` imports and uses `UNDERSTAT_LEAGUE_NAMES[league]` at scraper.py:727. |
| `scrape_understat_league` | `cache/understat_{league}_{season}.csv` | `_cache_path(f'understat_{league}_{season_label}')` | VERIFIED | Cache key `understat_EPL_2024-25` confirmed via live probe. Pattern matches spec exactly. |
| `build_dataset` | `attach_understat_xg` | called inside build_dataset loop before `compute_per90s()` | VERIFIED | merger.py:521-523 calls `attach_understat_xg` inside per-league loop; `compute_per90s` follows at line 526. |
| `attach_understat_xg` | rapidfuzz WRatio >= FUZZY_THRESHOLD | `process.extractOne` in Pass 2 | VERIFIED | merger.py:439-451: `scorer=fuzz.WRatio, score_cutoff=FUZZY_THRESHOLD`. `test_attach_understat_fuzzy_match` PASSES. |
| `run_scoring_pipeline` | `run_understat_scrapers` | direct call before `build_dataset` | VERIFIED | scorer.py:338-340: import, call, and pass to `build_dataset(understat_data=understat_data)` confirmed. |
| `PILLARS_FW[attacking][stats]` | `xG_p90` at 0.45 | scorer `_score_group` reads pillar stat keys | VERIFIED | config.py:198: `{"xG_p90": 0.45, "SoT_p90": 0.35, "Ast_p90": 0.20}`. |
| `PILLARS_MF[creation][stats]` | `xA_p90` at 0.50 | scorer `_score_group` reads pillar stat keys | VERIFIED | config.py:233: `{"xA_p90": 0.50, "Ast_p90": 0.30, "Crs_p90": 0.20}`. |

---

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|---------------|-------------|--------|----------|
| SCORE-01 | 06.1-01, 06.1-02, 06.1-03 | System computes position-specific pillar scores using existing weight structure, normalized via MinMaxScaler per league+position group | SATISFIED | FW/MF Attacking and Creation pillars updated with xG_p90/xA_p90. All pillar sums = 1.0. `test_fw_pillar_weights_sum`, `test_mf_pillar_weights_sum` PASS. |
| DATA-01 | 06.1-01, 06.1-02 | User can load player stats from FBref for all top 5 European leagues | SATISFIED | `run_understat_scrapers()` extends data coverage to all 5 leagues x 2 seasons. No regression to existing FBref scraper. |
| DATA-05 | 06.1-01 | System caches scraped data as CSV files with 7-day TTL | SATISFIED | Understat cache follows project convention: `cache/understat_{league}_{season}.csv`. 7-day TTL enforced via `_is_fresh`. `test_understat_cache_hit` PASSES. |
| DATA-06 | 06.1-01 | System applies polite scraping with rate limiting | SATISFIED | Understat fetches via single async aiohttp session (no Cloudflare). FBref rate limiting unchanged and tested. Note: Understat does not require the same 3.5-6s delay as FBref (different anti-bot posture), which is architecturally correct per PLAN comment. |

**No orphaned requirements** — all 4 IDs from PLAN frontmatter are accounted for. REQUIREMENTS.md does not map additional IDs to Phase 06.1.

**Note on ROADMAP.md:** The ROADMAP shows `- [ ] 06.1-03-PLAN.md` (unchecked) even though Plan 03 is complete and its SUMMARY.md exists. This is a documentation staleness issue only — the ROADMAP checkbox was not updated after Plan 03 completed. All code artifacts verify correctly.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scraper.py` | 959 | `DEPRECATED` marker in `run_api_football_scrapers()` | Info | Pre-existing deprecation for a different, unrelated function (`run_api_football_scrapers`). Does not affect this phase. `run_understat_scrapers` has no DEPRECATED marker. |

No blockers or warnings found in the phase-modified files.

---

### Human Verification Required

None. All success criteria are programmatically verifiable and confirmed.

---

### Summary

Phase 06.1 goal is fully achieved. All three plans executed cleanly:

**Plan 01** delivered `UNDERSTAT_LEAGUE_NAMES` in config.py, `xG`/`xA` in `SUM_STATS` and `PER90_STATS`, `scrape_understat_league()` with correct cache naming, and a real `run_understat_scrapers()` replacing the former deprecated stub. All 4 Wave 0 scraper tests pass.

**Plan 02** delivered `attach_understat_xg()` with 2-pass exact+fuzzy matching (WRatio >= 80), wired into `build_dataset(understat_data=None)` before `compute_per90s()`, and `run_scoring_pipeline()` updated to call `run_understat_scrapers()` and pass the result through. All 4 Wave 0 merger tests pass.

**Plan 03** delivered updated `PILLARS_FW` and `PILLARS_MF` Attacking and Creation pillar stats with xG_p90/xA_p90, weight-sum integrity tests, and CLAUDE.md documentation update. DF and GK pillars are unchanged.

The full test suite stands at **84 tests, all passing**, with 10 new tests covering the entire Understat integration surface.

---

_Verified: 2026-03-18T10:15:00Z_
_Verifier: Claude (gsd-verifier)_
